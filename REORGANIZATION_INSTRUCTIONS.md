# ИНСТРУКЦИЯ ПО РЕОРГАНИЗАЦИИ ПРОЕКТА COGNIFLEX

## 📋 Что было сделано:

### ✅ Анализ проекта:
1. **Проанализирована структура** - изучены все папки и файлы проекта
2. **Классифицированы файлы** - разделены на функциональные, отладочные, кэши и др.
3. **Выявлены зависимости** - проанализированы requirements.txt и импорты
4. **Создан лог-файл** - `project_structure_log.md` с полной информацией

### ✅ Создан план реорганизации:
- **Функциональный код** → останется в `cogniflex/`
- **Отладочные файлы** → переместятся в `debug/`
- **Кэши и данные** → переместятся в `cache/all_caches/`
- **Логи** → переместятся в `logs/`
- **Скрипты** → переместятся в `scripts/cleanup/`
- **Документация** → переместится в `docs/`
- **Конфигурация** → переместится в `config/`

## 🚀 Как запустить реорганизацию:

### Шаг 1: Запуск скрипта
```bash
cd C:\Users\black\OneDrive\Desktop\CogniFlex
python reorganize_project.py
```

### Шаг 2: Проверка результата
После выполнения скрипта проверьте новую структуру:
```
CogniFlex/
├── cogniflex/          # Основной функциональный код
├── debug/             # Отладочные файлы
├── logs/              # Логи системы
├── cache/
│   └── all_caches/    # Все кэши и данные
├── scripts/
│   └── cleanup/       # Скрипты для обслуживания
├── docs/              # Документация
├── config/            # Конфигурационные файлы
├── tests/             # Тестовые файлы
├── requirements.txt   # Зависимости
├── pyproject.toml     # Конфигурация проекта
└── reorganize_project.py  # Этот скрипт (можно удалить)
```

## 🔧 После реорганизации:

### Тестирование работоспособности:
```bash
# Проверьте, что система запускается
python -m cogniflex.run
```

### Если возникнут проблемы:
1. **Проверьте импорты** - возможно, нужно обновить пути в коде
2. **Проверьте конфигурацию** - убедитесь, что все пути корректны
3. **Запустите тесты** - проверьте работу основных функций

## 📁 Какие файлы будут перемещены:

### В debug/:
- system_selftest.py
- system_selftest.log
- test_fractal_store.py
- nlp_fallbacks.py
- dependency_report.log
- output.txt, output_log.txt
- test_generation_integration.log

### В cache/all_caches/:
- cogniflex_cache/
- ethics_cache/
- hybrid_cache/
- ml_cache/
- tokenizer_cache/
- search_cache.json
- base_knowledge_v2.json
- cogniflex_knowledge.db
- test_model.pt

### В logs/:
- logs/ (вся папка)

### В scripts/cleanup/:
- scripts/ (вся папка)

### В docs/:
- documentation/ (вся папка)

### В config/:
- universal_analyzer_config.ini
- pytest.ini

## ⚠️ Важные замечания:

1. **Резервное копирование** - перед запуском скрипта создайте резервную копию проекта
2. **Тестирование** - после реорганизации обязательно протестируйте запуск системы
3. **Импорты** - возможно, потребуется обновить пути импортов в коде
4. **Конфигурация** - проверьте все конфигурационные файлы на корректность путей

## 🎯 Следующие шаги после реорганизации:

1. **Тестирование системы** - убедитесь, что `python -m cogniflex.run` работает
2. **Очистка** - удалите `reorganize_project.py` и `project_structure_log.md`
3. **Документация** - обновите документацию с новой структурой
4. **CI/CD** - настройте автоматическую сборку и тестирование

---
*Создано Cascade AI Assistant для проекта CogniFlex*
